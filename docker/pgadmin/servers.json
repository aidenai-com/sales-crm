{
  "Servers": {
    "1": {
      "Name": "CRM (docker)",
      "Group": "Servers",
      "Comment": "The crm-postgres container. Password is 'crm'.",
      "Host": "db",
      "Port": 5432,
      "MaintenanceDB": "crm",
      "Username": "crm",
      "SSLMode": "prefer",
      "ConnectionParameters": {
        "sslmode": "prefer",
        "connect_timeout": 10
      }
    }
  }
}
